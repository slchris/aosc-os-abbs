# Fix format-security
# https://github.com/ukui/ukui-control-center/pull/6
patch -Np1 -i autobuild/patches/000-fix-format-security.patch

# Fix build with libkscreen 5.27
sed -e 's|c++11||' -i plugins/system/display/display.pro
patch -p1 -i autobuild/patches/001-libkscreen-5.27.patch

# Fix build with ddcutil 2
patch -p1 -i autobuild/patches/002-ddcutil-2.patch
