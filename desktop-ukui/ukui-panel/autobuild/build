abinfo "Building ukui-panel..."
mkdir -p build
cd build
cmake .. -GNinja -DCMAKE_INSTALL_PREFIX=/usr -DCMAKE_INSTALL_LIBDIR=lib
ninja

abinfo "Installing ukui-panel..."
DESTDIR="$PKGDIR" ninja install
